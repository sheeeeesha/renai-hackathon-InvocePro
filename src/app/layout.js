import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "InvoicePro",
  description: "InvoicePro is your ultimate invoice hack. Using AI, it scans PDFs, extracts key details like customer names, invoice numbers, and amounts, and organizes everything for you. Whether for a side hustle or big business, it’s fast, accurate, and easy to use—making invoicing effortless.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
